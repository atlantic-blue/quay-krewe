# The template for a system's configuration. It is written to ~/.krewe/env, which is where a system keeps
# what belongs to it on this machine. `make install` writes it on a first run, and never writes over
# it again. Every `make up` and `make upgrade` reads that same file, so an upgrade cannot quietly
# bring the stack back as something else.
#
#   make install         # writes ~/.krewe/env, builds everything, and brings the system up
#   make config          # writes ~/.krewe/env only, then edit it
#   make up              # brings the stack back on what you edited
#
# The file is outside this checkout on purpose. A system that was installed rather than cloned has no
# checkout to keep configuration in, and configuration that lives in one cannot be given to anybody.
# Set KREWE_HOME to put it somewhere else.
#
# It holds configuration, never secrets: the model token is set at runtime with `krewe secret set` and
# never written to disk here.

# The backend an exec runs against. "claude-code" runs the real thing on your subscription; "echo" runs
# `echo` inside the sandbox instead, which is what continuous integration uses because it has no
# subscription.
QC_MODEL=claude-code

# Which model that backend runs against. An alias for the newest of a tier ("opus", "sonnet") or a
# full name ("claude-opus-5"). Leave it out and the system still uses claude-opus-5, which is the
# default in the compose file.
#
# Without this the command line tool chooses for itself, and it chooses Sonnet, which is why every
# session was running Sonnet while the operator was paying for Opus.
QC_CLAUDE_MODEL=claude-opus-5

# What a session's execs may do without asking, when the session is born: plan, edits or dangerous.
#
# plan reads and proposes and changes nothing. edits lets it change the files in its own working
# directory. dangerous lets it do anything, which in a sandbox means anything to one container
# holding one project's files.
#
# A dispatched exec has nobody to approve anything, so a session born in a mode narrower than the work
# stops and asks somebody who is not there. `krewe mode <session>` changes one session after the fact;
# this is what every new one starts as.
#
# Unset keeps edits, which is what every session had before this could be set. An unknown value stops
# the system starting rather than quietly falling back.
QC_PERMISSION_MODE=edits

# How many execs past its own description a conversation goes before the system writes it again.
#
# A session describes itself after its first exec, so a listing reads as conversations rather than as
# identifiers without anybody naming anything. After that it is rewritten when the conversation has
# moved on this far. Each one costs a model call, in its own conversation, so it never lands in your
# history and never counts towards what the listing says the session cost.
#
# Ten is a starting number, not a measured one. Nothing has run long enough yet to say how far a
# conversation drifts per exec, so it is set here rather than presented as derived. What would replace
# it is a count of how often a rewrite actually differs from the description before it.
#
# "off" turns it off entirely, which a system running automation wants: a flow makes a session per run.
# Your own name for a session is never touched by any of this.
QC_DESCRIBE_EVERY=10

# The image a session's sandbox runs. Build it with `make sandbox-image`.
QC_SANDBOX_IMAGE=krewe-sandbox-claude:local

# How much memory one session may take, as the daemon spells it, for example 4g. Commented out gives
# a session no limit of its own, which is what every session had before this key existed.
#
# A sandbox with no limit reports the whole machine in /proc/meminfo. So node sizes its heap from it,
# Go sizes its collector from it, and jest and webpack start one worker for each processor, while
# what is actually there is whatever the rest of the machine has not taken. The session budgets
# against the first number and the kernel kills it against the second, with signal 9 and nothing
# said. Set this and the number a session reads is the number it is held to.
#
# It is a share of one machine between the stack, every session already running, and this one. Add up
# what you are willing to give them and check it against what the daemon has. `krewe room` inside a
# session reports whichever of the two it got.
# QC_SANDBOX_MEMORY=4g

# What the system keeps back for its own containers before it admits any sandbox: mebibytes, and per
# cent of one processor. The control plane, the database and the event log run inside the same
# runtime the work fills, so a system that reserves nothing goes down with its own workload, which is
# what happened on 30 August 2026.
#
# Unset takes the system's own floor, and what actually binds is measured: everything the runtime holds
# less what the sandboxes hold is the system's own. Measure what to put here on the machine the system
# runs on, with the system up and no jobs running:
#
#   docker stats --no-stream --format '{{.Name}}\t{{.MemUsage}}\t{{.CPUPerc}}' | grep -v quaycrew-
#
# QC_SYSTEM_RESERVE_MEMORY=2048
# QC_SYSTEM_RESERVE_PROCESSOR=200

# Where a workspace's conversations and a project's files are kept. Defaults to ~/.krewe/data.
# QC_DATA_HOST=/Volumes/work/quaycrew

# Where the tree of names is. Defaults to ~/.krewe/at, and that is the directory to open: ~/.krewe/at/itv
# is the shared folder of the workspace called itv, and ~/.krewe/at/itv/vast is the vast project's
# folder inside it. Every directory under the data directory is named after a generated identifier, and
# this is the view that carries the names instead. Deleting it loses the names and no work.
# QC_NAMES_HOST=/Volumes/work/krewe-names

# The system's token lives at <data directory>/system.token, minted by the control plane the first time
# it starts. Every caller has to present it; krewe reads it from that file on this machine, so there
# is nothing to set here. From another machine, or from a script, export QC_TOKEN with the file's
# contents. The port is published on loopback only; publish it wider only behind transport you own.
# The driver holds its own token, driver.token beside it, which the system refuses the calls that
# grant capability: a session that can drive the system cannot grant itself anything.

# Where the system is, as a sandbox dials it. Every sandbox is on a network that reaches this address
# and reaches nothing else of the system's: not the store, not the broker, not the dashboards. The
# compose file creates that network and puts the control plane on it, and the Makefile names it after
# this stack, so there is nothing to set for it here.
#
# Reaching the system is not permission to use it. A session is refused every call until it presents a
# credential the system minted for the job it is running, and that credential carries only the
# verbs the job's role declared and expires with the job. So a session on the network holding no
# credential can do nothing at all.
#
# Empty stops the system telling a session where it is, and then a session running a job holds a
# credential it cannot spend.
QC_SANDBOX_CONTROL_PLANE=controlplane:50051

# The system's own network, which only the driver joins. It carries Postgres, Redpanda and the
# observability stack, so this is a real widening and it is off unless you name one. Empty puts the
# driver on the same network as every other session, where it reaches the control plane, which is all
# it needs to drive the system.
#
# Set it to quaycrew_default, the network named after the compose project, if a driver on this machine
# has to reach the rest of the stack.
QC_SANDBOX_NETWORK=

# There is nothing here about which secrets a sandbox is given. A workspace's secrets reach that
# workspace's sandboxes, so `krewe secret set <workspace> <name> <value>` is the whole of it and a
# session is running with the value on its next exec. There used to be an allowlist here as well,
# which meant a secret could be set, and its skill attached, and the exec still refused for a reason
# that lived in this file. Names starting QC_ are the exception and never travel: those are the
# system's own configuration. See docs/SKILLS.md.

# The skills a session is given live in <data directory>/skills, one directory per skill, and the
# compose file finds them from QC_DATA_HOST. There is nothing to set here: put a skill in that
# directory and every session gets it. A skill is a directory with skill.yaml, SKILL.md and whatever
# else it needs. See docs/SKILLS.md.

# Who a commit made inside a sandbox is by. Both or neither: git wants an author and a committer and
# refuses on either missing, so half of this is the same as none of it and looks configured.
#
# `git` is in the sandbox image and unusable without this. A session asked to commit walks into
# "Please tell me who you are" halfway through whatever it was doing.
#
# QC_GIT_AUTHOR_NAME=Your Name
# QC_GIT_AUTHOR_EMAIL=you@example.com

# What the driver can see of this machine, as docker mounts, comma separated. The driver is the
# session that drives the system, and this is what makes it the glue between the machine and the system:
# without it the driver can reach the control plane and has nothing to bring to it.
#
# Only the driver gets these. Hand it what it needs and nothing else, and read only where it only has
# to read.
#
# QC_DRIVER_MOUNTS=/Users/you/claude:/home/agent/hub:ro

# How long the job controller holds a job before another controller may take it.
#
# Derived rather than chosen: the holder renews its hold on every tick while its exec is open, so
# what this has to outlast is a gap between renewals rather than an exec. A tick with nothing to do
# costs microseconds, so this is twelve tick intervals, which is the same budget the system already
# gives the longest healthy operation it has. Provisional until there are fifty completed jobs to
# measure the gap over.
#
# Unset takes that default. Set it longer only if this system's store is slow enough to miss twelve
# ticks in a row.
QC_JOB_LEASE=60s
